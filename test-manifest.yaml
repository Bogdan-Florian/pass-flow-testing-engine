name: "PASS Flow Validator"

database:
  connection_url: "XXXXXXXXXXXX"
suites:
  - name: "BKSP32 NB Input Flow"
    config: "flow_tests/bksp32-nb/config.yaml"
    enabled: true
    critical: true
    date_format: "%Y-%m-%d"
    tags: ["critical", "policy", "issuance"]

execution:
  parallel: false
  stop_on_critical_failure: false

reporting:
  output_dir: "reports"
  aggregate_report: "aggregate_summary.json"
  per_suite_reports: true